# Put your gallery photos here

Drop photos named to match `data/gallery.json`, e.g.:

- `photo1.jpg`
- `photo2.jpg`
- `photo3.jpg`

Then edit `data/gallery.json` to set the `caption` and `date` for each one.
Add as many `{ "image": ..., "caption": ..., "date": ... }` entries as you like.
